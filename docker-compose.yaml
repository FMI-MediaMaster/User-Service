services:
  <NAME>:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    image: <NAME>:latest
    hostname: <NAME>.mediamaster.internal
    container_name: <NAME>.mediamaster.internal
    volumes:
      - ./.env:/app/.env:ro
    networks:
      - mediamaster-internal-network

networks:
  mediamaster-internal-network:
    name: mediamaster-internal-network
